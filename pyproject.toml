[project]
name = "portfolio"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "markdown>=3.10",
    "pytest>=9.0.2",
    "python-fasthtml>=0.12.37",
    "ruff>=0.14.10",
]

[tool.ruff.lint]
ignore = ["F811"]
